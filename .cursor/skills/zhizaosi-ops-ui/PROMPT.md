# 开工 prompt

新项目第一句话粘贴下面整段。代理应读取并遵守 `zhizaosi-ops-ui` skill。

```
按织造司作业台与手持规范做这个新项目。先读 skill：zhizaosi-ops-ui。

这是仓内作业产品，不是营销页。有两套界面，规则不要混用：

后台作业台
- 画布 #f2f3f7，卡片和侧栏纯白，主色 #006de6，侧栏选中底 #dee9fb
- 字体 PingFang SC、冬青黑体、微软雅黑
- 筛选、页签、表格分成白卡
- 查询和新增是实心蓝按钮，重置是白底灰边
- 操作列是 12px 文字链：普通蓝色，取消/删除/停用红色 #e85a6b
- 12px 密度，行高约 36px，圆角 4px，细线图标
- 现场扫码动作只放在手持，后台显示状态，并注明由 PDA 回写

手持 PDA
- 宽屏必须是一台手机：390×844 黑外框、圆角、状态栏、底部返回/主页/最近任务
- 内容在手机屏幕里滚动，禁止把页面裁成空白或把按钮切掉
- 窄屏去掉外框，仍保留状态栏和底部操作栏
- 作业底栏贴在系统操作栏上方，不要固定到浏览器窗口
- 操作列全部是实心蓝按钮，文字必须完整，不能只露出一个字
- 单号、SKU、唯一码、库位等数据点击就复制，并提示已复制
- 扫码框内右侧放「扫码」；失败红条，成功短提示
- 改完静态 css/js 要换新的 ?v= 缓存参数

交付前用约 390px 宽检查操作按钮、顶栏和主内容；宽屏再看一遍手机轮廓。不要使用 #1890ff、紫渐变、大留白或整页一张大白卡。
```
