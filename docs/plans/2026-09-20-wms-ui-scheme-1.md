# WMS 视觉与交互修正方案（方案 1）

- **批准日期：** 2026-09-20
- **状态：** 已经过用户批准（方案 1：浅色侧栏按 WMS 目录图做透）
- **视觉修订：** 2026-09-20 起配色与选中态以 `2026-09-20-wms-visual-yingce.md` 为准（灰画布 + 白卡片 + 中性选中 + 蓝只做 CTA）；本文件保留侧栏信息架构与列表骨架约束
- **范围：** `wms/basic` 三页壳层 + 列表作业流

## 选定方向

浅色侧栏（用户目录截图）+ 列表骨架不变；工作台视觉改为灰画布上的白卡片（见影策修订案）。

## 必须落地

1. **侧栏：** 浅底、分组折叠、图标；选中为中性灰圆角条（不再用浅蓝底 / 右侧蓝竖线）  
2. **列表页骨架：** 筛选行 → 操作行（主按钮在此；勾选后出现批量）→ 表 → 分页  
3. **主色收敛：** 蓝仅用于主按钮（CTA）与文字链  
4. **表格：** 状态分段（如全部/启用/停用，按实体裁剪）、行内「更多」收纳次要操作  
5. **去掉装饰：** 弱化/去掉演示感 logo 块与无意义头像装饰感  

## 非目标

- 不引入深色顶栏/深色一级模块栏  
- 不改业务表结构与 mock 字段含义  
